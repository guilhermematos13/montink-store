'use client';

import { Button } from '@/components/Button';
import { HomeWrapperProps } from './types';

export function HomeWrapper({ products }: HomeWrapperProps) {
  return (
    <>
      <div>Home Wrapper</div>
      <Button>Teste</Button>
    </>
  );
}
